#include<bits/stdc++.h>
using namespace std;
vector<int>jora(vector<int>a)
{
    vector<int>ara1;
    vector<int>ara2;
    int i;
    for ( i = 0; i < a; i++)
    {
        int input;
        cin>>input;
        ara1.push_back(input);
    }
    cout<<"end of first array input"<<"\n";
    for ( i = 0; i < ara1.size(); i++)
    {
        cout<<ara1[i]<<" ";
    }
    cout<<"end of first array"<<"\n";
    for ( i = 0; i < b; i++)
    {
        int input;
        cin>>input;
        ara2.push_back(input);
    }
    cout<<"end of 2 array"<<"\n";
    for ( i = 0; i < ara2.size(); i++)
    {
        cout<<ara2[i]<<" ";
    }

}
int main()
{
    int a,b;
    
    
    
    return 0;
}